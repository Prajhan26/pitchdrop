{
  "name": "@pitchdrop/indexer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "prisma generate && prisma db push && tsx src/index.ts",
    "seed": "tsx src/seed.ts",
    "build": "prisma generate && tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@pitchdrop/shared": "workspace:*",
    "fastify": "^4.28.0",
    "@fastify/websocket": "^10.0.0",
    "@fastify/cors": "^9.0.0",
    "prisma": "^5.22.0",
    "@prisma/client": "^5.22.0",
    "viem": "^2.21.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@repo/eslint-config": "workspace:*",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
