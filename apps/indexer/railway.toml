[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @pitchdrop/shared build && pnpm --filter @pitchdrop/indexer build"

[deploy]
startCommand = "pnpm --filter @pitchdrop/indexer start"
restartPolicyType = "on_failure"
